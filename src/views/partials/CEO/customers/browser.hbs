<center>
  <table class="table">
    <colgroup>
        <col style="width: 40%;">
        <col style="width: 40%;">
        <col style="width: 5%;">
        <col style="width: 8%;">
        <col style="width: 10%;">
    </colgroup>
    <thead>
      <tr>
        <th>
            <label for=""><i class="fi-icon fi-sr-envelope"></i> customer email</label>
            <input type="text" class="form-control" placeholder="customer email" name="email">
        </th>
        <th>
            <label for=""><i class="fi-icon fi-ss-flag"></i> Country</label>
            <select class="form-control">
                <option value="-1" id="-1">All</option>
                {{#each country}}
                    <option value="{{id}}" id="{{id}}">{{name}}</option>
                {{/each}}
            </select>
        </th> 
        <th>
            <br>
            <button class="btn"><i class="fi-icon fi-rr-search"></i></button>
        </th>
        <th>
            <br>
            {{#each company}}
              <button class="btn-add" id="{{id}}" onclick="nextWeb(this.id+'/add-customer')">+</button>
            {{/each}}
        </th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <tr>
      </tr>
    </tbody>
  </table>
</center>